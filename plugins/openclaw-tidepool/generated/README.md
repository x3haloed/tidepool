This directory holds vendored, generated SpacetimeDB client bindings for the OpenClaw Tidepool plugin.

These files are intentionally kept inside the plugin package so the plugin remains self-contained when installed locally or shipped independently.

Expected generated output location:

`plugins/openclaw-tidepool/generated/module_bindings/`
