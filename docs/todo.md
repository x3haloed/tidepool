# To-dos

- [ ] validate the OpenClaw Tidepool plugin against the real OpenClaw plugin SDK contract
- [ ] wire `seedDomainIds` and `batchWindowSeconds` into the plugin runtime or remove them from the config surface
- [ ] decide whether the plugin should ship raw TypeScript entries or a built JavaScript artifact
